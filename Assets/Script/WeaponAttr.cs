using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WeaponAttr : MonoBehaviour
{
    public GameObject frontHandler;
    public GameObject backHandler;

    public float attack;      // 攻击力
    public float shootSpeed;  // 射速
    public float bulletSpeed; // 子弹速度
    public float bulletNum;   // 装弹量
    public float currentBulletNum; // 当前装弹量
    public float backup;      // 后备弹夹
    public float road_time;   // 装弹时间


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
